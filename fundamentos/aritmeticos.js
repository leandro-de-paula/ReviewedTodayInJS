const [a, b, c, d, e] = [3, 5, 1, 15, 8]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b 
const divisao = d / a
const modulo = a % 2
const modulo2 = e % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo, modulo2)